version: 0.0
os: linux
files:
   - source: /
     destination: /var/www/restx
hooks:
   BeforeInstall:
    - location: scripts/startapache.sh
      runas: root
   AfterInstall:
   - location: cd /var/www/restx
      runas: root
   - location: sh start.sh
      runas: root
